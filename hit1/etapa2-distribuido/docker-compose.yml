version: '3.8'

services:
  rabbitmq:
    image: rabbitmq:management
    ports:
      - "5672:5672"
      - "15672:15672"
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "ping"]
      interval: 10s
      timeout: 5s
      retries: 10
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq

  worker:
    build:
      context: .
      dockerfile: Dockerfile.worker
    depends_on:
      rabbitmq:
        condition: service_healthy
    environment:
      RABBITMQ_HOST: rabbitmq
    volumes:
      - ./logs:/app/logs
    deploy:
      replicas: 4

  master:
    build:
      context: .
      dockerfile: Dockerfile.master
    depends_on:
      rabbitmq:
        condition: service_healthy
    environment:
      RABBITMQ_HOST: rabbitmq
      NUM_CHUNKS: "4"
      INPUT_IMAGE: /app/images/maravilla.jpeg
      OUTPUT_IMAGE: /app/images/resultado.jpg
    volumes:
      - ./images:/app/images
      - ./logs:/app/logs
    profiles: ["master"]

volumes:
  rabbitmq_data:
