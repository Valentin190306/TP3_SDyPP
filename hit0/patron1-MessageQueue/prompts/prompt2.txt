CONTEXTO
========
Tengo implementado el Patrón 1 (Message Queue punto a punto) del HIT #0 El código ya funciona.
Debo agregarle dos funcionalidades: logging en memoria y disco, y pruebas unitarias
y de integración con pytest.

ESTRUCTURA ACTUAL DEL PROYECTO
[adjuntar]

ESTRUCTURA FINAL REQUERIDA
===========================
TP3_SDyPP/hit0/patron1-MessageQueue/
├── tests/
│   ├── __init__.py
│   ├── test_producer.py
│   └── test_integration.py
├── logs/                        ← creado en runtime, NO commitear
├── logger.py                    ← archivo nuevo
├── producer.py                  ← refactorizar
├── consumer.py                  ← modificar
├── Dockerfile.producer          ← sin cambios
├── Dockerfile.consumer          ← sin cambios
├── docker-compose.yml           ← agregar volumen logs
├── requirements.txt             ← sin cambios
├── requirements-dev.txt         ← archivo nuevo
└── README.md                    ← actualizar sección de ejecución

TAREA 1: CREAR logger.py
=========================
Crear un módulo logger.py en la raíz del proyecto con una función get_logger(name).

Especificaciones:
- Función: get_logger(name: str) -> logging.Logger
- Level: DEBUG
- Formato: '%(asctime)s | %(name)s | %(levelname)s | %(message)s'
- datefmt: '%Y-%m-%dT%H:%M:%S'
- Dos handlers:
  1. StreamHandler → stdout (memoria / consola)
  2. FileHandler → '/app/logs/{name}.log' (disco)
- Crear directorio /app/logs con os.makedirs(..., exist_ok=True) antes del FileHandler
- Evitar agregar handlers duplicados: verificar con "if not logger.handlers" antes de 
  agregar los handlers
- No usar logging.basicConfig(), configurar handlers manualmente

TAREA 2: REFACTORIZAR producer.py
==================================
Modificar producer.py para que sea testeable. Cambios requeridos:

1. Importar y usar logger.py:
   from logger import get_logger
   logger = get_logger('producer')

2. Reemplazar todos los print() por llamadas al logger:
   - Conexión establecida → logger.info()
   - Cada intento de retry fallido → logger.warning() con número de intento
   - Fallo total de conexión → logger.error()
   - Cada mensaje enviado → logger.info()

3. Extraer la lógica de envío a una función separada send_messages(channel):
   - Recibe un channel ya conectado como parámetro
   - Declara la cola 'task_queue' con durable=True dentro de la función
   - Envía los 10 mensajes con el loop existente
   - Loggea cada mensaje enviado
   - No abre ni cierra conexión (eso queda en el bloque main)

4. Mantener el bloque if __name__ == '__main__': que llama a connect() y luego 
   a send_messages() con el channel, y cierra la conexión al finalizar.

La firma exacta de send_messages debe ser:
def send_messages(channel) -> None

TAREA 3: MODIFICAR consumer.py
================================
Cambios requeridos en consumer.py:

1. Importar y usar logger.py:
   from logger import get_logger
   logger = get_logger('consumer')

2. Reemplazar todos los print() por llamadas al logger:
   - Consumer listo y escuchando → logger.info()
   - Cada mensaje recibido → logger.info() con CONSUMER_ID y body
   - Cada intento de retry fallido → logger.warning()
   - Fallo total de conexión → logger.error()

No refactorizar la estructura del consumer, solo agregar logging.

TAREA 4: MODIFICAR docker-compose.yml
=======================================
Agregar en los servicios 'producer' y 'consumer' la siguiente entrada bajo cada 
definición de servicio:

volumes:
  - ./logs:/app/logs

No modificar ninguna otra configuración del compose.

TAREA 5: CREAR requirements-dev.txt
=====================================
Archivo con exactamente estas dependencias:
pika>=1.3.0
pytest>=7.0
pytest-timeout>=2.1

TAREA 6: CREAR tests/__init__.py
==================================
Archivo vacío. Solo crearlo.

TAREA 7: CREAR tests/test_producer.py
=======================================
Pruebas unitarias que NO requieren RabbitMQ corriendo. Usar unittest.mock.

Importaciones necesarias:
- pytest
- from unittest.mock import patch, MagicMock, call
- import pika
- import sys, os
- sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), '..')))
- import producer

Tests a implementar — exactamente estos 6:

TEST 1: test_send_messages_declara_cola_correctamente
- Mockear pika.BlockingConnection
- Llamar producer.send_messages(mock_channel)
- Verificar que mock_channel.queue_declare fue llamado con 
  queue='task_queue' y durable=True

TEST 2: test_send_messages_envia_exactamente_10_mensajes
- Mockear pika.BlockingConnection
- Llamar producer.send_messages(mock_channel)
- Verificar que mock_channel.basic_publish.call_count == 10

TEST 3: test_send_messages_formato_correcto_de_mensajes
- Mockear pika.BlockingConnection
- Llamar producer.send_messages(mock_channel)
- Extraer el argumento 'body' de cada call en basic_publish.call_args_list
- Verificar que los bodies son exactamente ["Tarea 1", "Tarea 2", ..., "Tarea 10"]

TEST 4: test_send_messages_delivery_mode_persistente
- Mockear pika.BlockingConnection
- Llamar producer.send_messages(mock_channel)
- Verificar que en cada llamada a basic_publish, el argumento properties
  tiene delivery_mode == 2

TEST 5: test_connect_reintenta_ante_fallo
- Mockear pika.BlockingConnection para que lance 
  pika.exceptions.AMQPConnectionError en los primeros 2 intentos y 
  luego retorne un mock válido en el tercero
- Mockear time.sleep para que no espere realmente
- Llamar producer.connect()
- Verificar que pika.BlockingConnection fue llamado exactamente 3 veces

TEST 6: test_connect_falla_despues_de_max_intentos
- Mockear pika.BlockingConnection para que siempre lance 
  pika.exceptions.AMQPConnectionError
- Mockear time.sleep
- Verificar que producer.connect() lanza RuntimeError o SystemExit 
  después de agotar los reintentos

Para los mocks de channel en cada test, usar este patrón:
mock_channel = MagicMock()
mock_channel.queue_declare.return_value = None

TAREA 8: CREAR tests/test_integration.py
==========================================
Pruebas de integración que SÍ requieren RabbitMQ corriendo.
Usar el mecanismo skipif para saltearlas automáticamente si no hay broker.

Importaciones necesarias:
- pytest
- pika
- os
- sys path insert igual que en test_producer.py
- import producer

Constante al inicio del archivo:
RABBIT_HOST = os.getenv('RABBITMQ_HOST', 'localhost')

Función helper (no es un test):
def rabbitmq_available() -> bool:
  Intenta crear una BlockingConnection a RABBIT_HOST.
  Retorna True si conecta, False si lanza cualquier excepción.
  Cerrar la conexión si fue exitosa antes de retornar.

Decorator a usar en todos los tests de integración:
requires_rabbit = pytest.mark.skipif(
    not rabbitmq_available(),
    reason="RabbitMQ no disponible"
)

Tests a implementar — exactamente estos 3:

TEST 1: test_producer_publica_10_mensajes_en_cola
- Decorar con @requires_rabbit
- Conectar directamente con pika a RABBIT_HOST
- Declarar 'task_queue' con durable=True
- Purgar la cola con queue_purge('task_queue')
- Llamar producer.send_messages(channel)
- Declarar la cola con passive=True para leer el message_count
- Verificar que message_count == 10
- Cerrar conexión

TEST 2: test_consumer_procesa_mensajes_correctamente
- Decorar con @requires_rabbit y @pytest.mark.timeout(10)
- Conectar con pika
- Declarar y purgar 'task_queue'
- Publicar manualmente 3 mensajes: "Tarea 1", "Tarea 2", "Tarea 3"
- Definir callback que acumula bodies en una lista y llama 
  channel.stop_consuming() cuando len(recibidos) == 3
- Llamar basic_consume y start_consuming()
- Verificar que recibidos == ["Tarea 1", "Tarea 2", "Tarea 3"]
- Cerrar conexión

TEST 3: test_cola_es_durable
- Decorar con @requires_rabbit
- Conectar con pika
- Declarar 'task_queue' con passive=True
- Verificar que la declaración no lanza excepción (la cola existe y es durable)
- Si lanza ChannelClosedByBroker, el test falla con mensaje 
  "La cola no existe o no es durable"
- Cerrar conexión

TAREA 9: ACTUALIZAR README.md
==============================
Agregar una nueva sección llamada "Pruebas y Logging" después de la sección 
"Cómo ejecutar". Contenido:

Subsección "Ejecutar pruebas unitarias":
  pip install -r requirements-dev.txt
  pytest tests/test_producer.py -v

Subsección "Ejecutar pruebas de integración":
  docker compose up rabbitmq -d
  pytest tests/test_integration.py -v

Subsección "Ejecutar todas las pruebas":
  pytest tests/ -v

Subsección "Logs":
  Los logs se generan en ./logs/ en el host (montado como volumen Docker).
  producer.log → actividad del producer
  consumer.log → actividad de cada consumer
  Formato: timestamp | nombre | nivel | mensaje

Subsección "Archivos ignorados":
  Indicar que ./logs/ está en .gitignore

TAREA 10: ACTUALIZAR .gitignore
=================================
Verificar que existe un .gitignore en la raíz del proyecto TP3_SDyPP.
Agregar estas líneas si no están presentes:
logs/
*.log
__pycache__/
*.pyc
.pytest_cache/
.env

RESTRICCIONES GENERALES
========================
- No modificar Dockerfile.producer ni Dockerfile.consumer
- No modificar requirements.txt
- No cambiar la lógica de negocio existente en producer.py ni consumer.py,
  solo refactorizar y agregar logging
- Todos los tests deben poder correr con: pytest tests/ -v
- Los tests unitarios deben pasar SIN RabbitMQ corriendo
- Los tests de integración deben saltearse automáticamente (skip, no fail) 
  si RabbitMQ no está disponible
- El logger no debe romper el funcionamiento dentro de los contenedores Docker
  (el path /app/logs debe crearse con exist_ok=True)
- No usar pytest fixtures complejos, mantener los tests simples y directos

ORDEN DE IMPLEMENTACIÓN
========================
Implementar en este orden para evitar errores de importación durante el desarrollo:
1. logger.py
2. Refactorizar producer.py
3. Modificar consumer.py
4. tests/__init__.py
5. tests/test_producer.py
6. tests/test_integration.py
7. requirements-dev.txt
8. docker-compose.yml (agregar volumen)
9. README.md
10. .gitignore

Genera todos los archivos completos y funcionales. No uses placeholders ni 
comentarios TODO. Cada archivo debe estar listo para usar sin modificaciones.
